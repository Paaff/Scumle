<RibbonWindow x:Class="Scumle.View.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:local="clr-namespace:Scumle.View"
         
         Title="Window1"
         Height="600"
         Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ControlTemplate x:Key="template1">
                <Ellipse Width="60" Height="30" Fill="Black"/>
            </ControlTemplate>
        </ResourceDictionary>
    </Window.Resources>



    <DockPanel LastChildFill="True">



        <local:Ribbon DockPanel.Dock="Top"/>
         
        

        <Border  DockPanel.Dock="Bottom" BorderThickness="2" BorderBrush="Black">
            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"> Status / Error Message</TextBlock>
        </Border>

        <StackPanel DockPanel.Dock="Left" Width="175" >
            <Expander Header="Arrows">

                <StackPanel>
                    <Rectangle Width="10" Height="10" Fill="YellowGreen"></Rectangle>
                </StackPanel>

            </Expander>

            <Label Content="{Binding Version}" Height="25" HorizontalAlignment="Center"></Label>

        </StackPanel>

    

        <ItemsControl ItemsSource="{Binding Shapes}" Background="AliceBlue">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!--<Rectangle Width="{Binding Width}" Height="{Binding Height}" Fill="Black" />-->
                    <local:Shape DataContext="{Binding}"></local:Shape>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </DockPanel>

</RibbonWindow>