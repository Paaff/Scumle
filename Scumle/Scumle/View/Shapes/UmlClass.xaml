<UserControl x:Class="Scumle.View.UmlClass"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Scumle.View"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200">
    
    
    <StackPanel Background="{Binding ShapeColor}">
        
         <!-- Name of the Shape (Class Name) -->
        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" 
                   HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"  Background="IndianRed" />
        
        <!-- Contents of the Class Diagram -->
        <Expander Header="Fields" >            
            <StackPanel Orientation="Vertical">
                <ListBox ItemsSource="{Binding UMLFields}"/>
                <Button Content="Added new Field" Command="{Binding addFieldCommand}" CommandParameter="Adding Field"/>
                <Button Content="Delete Field"/>
            </StackPanel>

        </Expander>
        <Expander Header="Methods">
            <StackPanel Orientation="Vertical">
                <ListBox ItemsSource="{Binding UMLMethods}"/>
                <Button Content="Added new Method" Command="{Binding addMethodCommand}" CommandParameter="Adding Method"/>
                <Button Content="Delete Method"/>
            </StackPanel>
          
        </Expander>

    </StackPanel>
    
    
  <!--  <StackPanel Background="{Binding ShapeColor}">
        <Label Content="{Binding Name}" FontSize="12"></Label>
        <TextBox AcceptsReturn="True" TextWrapping="Wrap" />
    </StackPanel>-->
</UserControl>
